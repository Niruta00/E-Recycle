import 'package:flutter/material.dart';

class ChoosePickUp extends StatelessWidget {
  const ChoosePickUp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text("Choose Pick Up"),
          
        ],
      ),
    );
  }
}
